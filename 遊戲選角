import sys

data = list(map(int, sys.stdin.read().split()))
n = data[0]

roles = []
idx = 1
for _ in range(n):
    a = data[idx]
    d = data[idx + 1]
    idx += 2
    score = a*a + d*d
    roles.append((score, a, d))

roles.sort(reverse=True)  # 分數由大到小
print(roles[1][1], roles[1][2])  # 第二高
